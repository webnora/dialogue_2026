# Мониторинг обучения BERT: Полное руководство

**Дата запуска**: 1 февраля 2026, 12:52
**Процесс**: PID 11791
**Корпус**: `cleaned_guardian_filtered.csv` (48,140 текстов)
**Ожидаемое время**: ~2.5-3 часа

---

## 🚀 Быстрая проверка статуса

### 1. Проверить, что процесс жив

```bash
ps aux | grep "run_bert_training.py" | grep -v grep
```

**Ожидаемый результат**:
```
nora  11791  X.X  X.X  427544832  XXXXXX  ??  UN  12:52PM   X:XX.XX python notebooks/run_bert_training.py
```

Если процесс есть → всё идёт ✅
Если пусто → процесс умер ❌ (проверьте лог на ошибки)

---

### 2. Посмотреть текущий прогресс (одна команда)

```bash
tail -20 results/bert_training_log.txt
```

**Что искать в выводе**:
- `Epoch 1/3`, `Epoch 2/3`, `Epoch 3/3` - номер текущей эпохи
- `Training: XX%` - прогресс внутри эпохи
- `loss=X.XXXX` - текущий loss (должен уменьшаться)
- `Val Accuracy: X.XXXX` - точность на валидации (появляется после каждой эпохи)

---

## 📊 Детальный мониторинг

### Вариант A: Следить в реальном времени (рекомендую)

```bash
tail -f results/bert_training_log.txt
```

**Как читать вывод**:
- **Training phase**: `Training: 25%|████▌         | 602/2407` (25% эпохи завершено)
- **Loss**: `loss=0.5234, avg_loss=0.6123` (текущий и средний loss)
- **Validation**: `Val Accuracy: 0.8765` (после каждой эпохи)
- **Epoch complete**: `✓ New best model! Saving...` (сохранение модели)

**Нажмите `Ctrl+C` чтобы выйти из режима follow** (это НЕ прервёт обучение!)

---

### Вариант B: Периодическая проверка

```bash
# Каждую минуту показывает последние 5 строк лога
watch -n 60 'tail -5 results/bert_training_log.txt'
```

**Нажмите `Ctrl+C` чтобы остановить watch**

---

### Вариант C: Проверять только ключевые метрики

```bash
# Показать только эпохи и точность
grep -E "Epoch|Val Accuracy|Testing|Final" results/bert_training_log.txt | tail -10
```

---

## 🔍 Понимание этапов обучения

### Этап 1: Токенизация (2-3 минуты)
```
[6/8] Tokenizing...
Map: 100%|██████████| 38512/38512 [04:05<00:00, 157.43 examples/s]
```
- Прогресс-бар должен дойти до 100%
- 38,512 train + 4,814 val + 4,814 test = 48,140 текстов

---

### Этап 2: Обучение (2-2.5 часа)

```
Epoch 1/3
Training: 10%|████         | 241/2407 [05:03<45:12, 1.27s/it]
```

**Как читать**:
- `10%` - 10% эпохи выполнено
- `241/2407` - batch 241 из 2407
- `[05:03<45:12]` - прошло 5 мин, осталось 45 мин
- `1.27s/it` - 1.27 секунды на batch

**Хороший знак** → loss падает (1.68 → 1.20 → 0.85 → 0.50 → ...)
**Плохой знак** → loss растёт или NaN

---

### Этап 3: Валидация (каждую эпоху)

```
Validating: 100%|██████████| 301/301 [01:23<00:00,  3.61s/it]

  Train Loss: 0.4523
  Val Accuracy: 0.8756
  Val F1 (macro): 0.8765

  ✓ New best model! Saving...
```

- Val Accuracy должен расти каждую эпоху
- Модель сохраняется только если accuracy улучшился

---

### Этап 4: Тестирование (в самом конце)

```
================================================================================
Testing on hold-out set...
================================================================================

Final Test Results:
  Accuracy: 0.8764
  F1 (macro): 0.8771
  F1 (weighted): 0.8771
```

**Когда увидите "Testing"** → осталось 1-2 минуты! 🎉

---

## 📁 Файлы, которые создаются

### В процессе обучения:
```
results/bert_training_log.txt          # Лог обучения (растёт до ~500KB-1MB)
models/bert_label_encoder_cleaned.pkl  # Label encoder (создаётся сразу)
```

### После завершения:
```
models/bert_category_classifier_cleaned/
├── config.json              # Конфигурация модели
├── model.safetensors        # Веса модели (~437MB)
├── tokenizer_config.json    # Конфигурация токенизатора
├── tokenizer.json           # Словарь токенизатора
└── vocab.txt                # Словарь BERT (30K слов)

results/bert_metrics_cleaned.json           # Финальные метрики
results/bert_confusion_matrix_cleaned.npy   # Confusion matrix
```

**Проверка создания модели**:
```bash
ls -lh models/bert_category_classifier_cleaned/
```

Если директория существует → модель обучена и сохранена! ✅

---

## ⚠️ Диагностика проблем

### Проблема 1: Процесс умер

**Симптомы**:
```bash
ps aux | grep run_bert_training  # Пустой вывод
```

**Что делать**:
```bash
# 1. Проверить конец лога на ошибки
tail -50 results/bert_training_log.txt

# 2. Поиск ошибок Python
grep -i "error\|exception\|traceback" results/bert_training_log.txt

# 3. Проверить память/диск
df -h  # Место на диске (нужно >5GB свободных)
```

**Частые ошибки**:
- `OutOfMemoryError` → недостаточно RAM
- `CUDA out of memory` → GPU память переполнена (уменьшите BATCH_SIZE)
- `FileNotFoundError` → неправильный путь к данным

---

### Проблема 2: Loss не падает

**Симптомы**: loss остаётся высоким (1.5-1.7) или растёт

**Что делать**:
```bash
# Посмотреть на динамику loss за последние 100 batch
grep "loss=" results/bert_training_log.txt | tail -100
```

**Нормально**: loss плавно уменьшается (1.68 → 1.50 → 1.30 → ...)
**Плохо**: loss прыгает хаотично или растёт

**Решения**:
- Подождать до конца эпохи (иногда improves в конце)
- Если после 3-х эпох loss > 1.0 → проблема с данными/гиперпараметрами

---

### Проблема 3: Обучение зависло

**Симптомы**: Лог не обновляется >5 минут

**Проверка**:
```bash
# 1. Проверить время последнего изменения лога
ls -l results/bert_training_log.txt

# 2. Проверить, что процесс не в состоянии D (uninterruptible sleep)
ps aux | grep run_bert_training
```

**Статусы процесса**:
- `R`/`RN` → Running, всё ок ✅
- `S`/`SN` → Sleeping (I/O), нормально ✅
- `D` → Uninterruptible sleep, подозрительно ⚠️
- `Z` → Zombie, умер ⚠️

---

## 📈 Расчёт времени обучения

### Прогноз на основе текущей скорости

```python
# В Python-shell:
import re

with open('results/bert_training_log.txt') as f:
    content = f.read()

# Найти текущий batch
matches = re.findall(r'(\d+)/2407', content)
if matches:
    current_batch = int(matches[-1])
    total_batches = 2407 * 3  # 3 эпохи
    progress = current_batch / total_batches

    print(f"Текущий batch: {current_batch}")
    print(f"Прогресс: {progress*100:.1f}%")
    print(f"Осталось: {(1-progress)*100:.1f}%")
```

---

### Примерный расчёт по времени

```bash
# Начало обучения: 12:52
# Текущее время: сейчас
# Эпох: 3
# Batch на эпоху: 2407
# Скорость: ~1.3s/batch

# Общее время = 2407 × 3 × 1.3 / 3600 = ~2.6 часа
# Финиш примерно: 12:52 + 2ч 36мин = 15:28
```

---

## 🎯 Что signalling завершение

### Успешное завершение ✅

В логе увидите:
```
================================================================================
Testing on hold-out set...
================================================================================

Final Test Results:
  Accuracy: 0.XXXX
  F1 (macro): 0.XXXX
  F1 (weighted): 0.XXXX

================================================================================
Results saved:
  Model: models/bert_category_classifier_cleaned/
  Metrics: results/bert_metrics_cleaned.json
  Confusion Matrix: results/bert_confusion_matrix_cleaned.npy
  Label Encoder: models/bert_label_encoder_cleaned.pkl
================================================================================
```

**Проверка**:
```bash
# 1. Процесс завершён
ps aux | grep run_bert_training  # Должен быть пустой

# 2. Модель создана
ls -lh models/bert_category_classifier_cleaned/

# 3. Метрики записаны
cat results/bert_metrics_cleaned.json
```

---

### Обучение завершилось с ошибкой ❌

**Симптомы**:
- Процесс умер
- В логе есть `Traceback` или `Error`

**Что делать**:
```bash
# 1. Сохранить лог для анализа
cp results/bert_training_log.txt results/bert_training_log_FAILED.txt

# 2. Перезапустить обучение
python3 notebooks/run_bert_training.py > results/bert_training_log.txt 2>&1 &
```

---

## 🛠️ Полезные команды (cheat sheet)

```bash
# === Быстрая проверка ===
# Процесс жив?
ps aux | grep run_bert_training | grep -v grep

# Последние 20 строк лога
tail -20 results/bert_training_log.txt

# === Мониторинг ===
# Следить в реальном времени
tail -f results/bert_training_log.txt

# Проверять каждые 30 секунд
watch -n 30 'tail -10 results/bert_training_log.txt'

# Только метрики
grep -E "Epoch|Val Accuracy|Loss" results/bert_training_log.txt | tail -20

# === Диагностика ===
# Размер лога
ls -lh results/bert_training_log.txt

# Модель создана?
ls -la models/bert_category_classifier_cleaned/

# Найти ошибки
grep -i "error\|exception" results/bert_training_log.txt

# === Статистика ===
# Сколько строк в логе
wc -l results/bert_training_log.txt

# Найти все упоминания эпох
grep "Epoch" results/bert_training_log.txt
```

---

## 📞 Если что-то пошло не так

### Не прерывайте процесс! Сначала проверьте:

1. **Посмотрите лог полностью**:
   ```bash
   less results/bert_training_log.txt
   # Навигация: ↓ вверх, ↓ вниз, /pattern поиск, q выход
   ```

2. **Проверьте диск**:
   ```bash
   df -h
   # Нужно >5GB свободных для модели
   ```

3. **Проверьте RAM**:
   ```bash
   vm_stat | head -10
   # Или через Activity Monitor
   ```

4. **Если процесс завис (D-состояние >10 мин)**:
   ```bash
   # Можно убить, но лучше дождаться
   kill -9 11791  # PID процесса
   ```

---

## 📝 После завершения обучения

### 1. Проверить результаты

```bash
# Метрики
cat results/bert_metrics_cleaned.json

# Ожидаемый вывод:
{
  "test_accuracy": 0.8764,
  "test_f1_macro": 0.8771,
  "test_f1_weighted": 0.8771,
  "best_val_accuracy": 0.8800,
  "categories": ["Analytical", "Editorial", "Feature", "News", "Review"],
  ...
}
```

---

### 2. Сравнить с предыдущими моделями

```bash
# Все метрики
ls results/*metrics*.json

# TF-IDF baseline
cat results/tfidf_metrics.json

# BERT (старый)
cat results/bert_metrics.json

# BERT (новый, filtered)
cat results/bert_metrics_cleaned.json
```

---

### 3. Использовать модель для классификации

```bash
python3 classify_texts.py \
  --model models/bert_category_classifier_cleaned \
  --label_encoder models/bert_label_encoder_cleaned.pkl \
  --text "Your text here..."
```

---

## 🎓 Дополнительная информация

### Скорость обучения на разных устройствах

| Устройство | Время на batch | Общее время |
|------------|---------------|-------------|
| MPS (M1/M2) | ~1.3s | ~2.5-3 часа |
| NVIDIA RTX 3090 | ~0.2s | ~25-30 мин |
| CPU | ~8s | ~16-18 часов |

**Ваш текущий**: MPS (Apple Silicon) ✅

---

### Типичный loss plot (если построить график)

```
Loss
1.6 |●
1.4 |  ●
1.2 |    ●
1.0 |      ●
0.8 |        ●●
0.6 |          ●●●
0.4 |            ●●●●●
0.2 |________________●●
    0    1000   2000   3000  Batch
```

**Нормально** → плавное снижение
**Аномально** → резкие скачки или рост

---

## ⏰ Планирование времени

### Ориентировочное время завершения

```bash
# Текущее время:
date

# Прибавить ~2.5 часа
# Пример: запустили в 12:52 → финиш в 15:22
```

### Рекомендации:
- 🌙 **Запускать на ночь** → утром готово
- ☕ **Отойти на кофе** → 30 мин не хватит
- 📚 **Заняться другими задачами** → самое лучшее!

---

## ✅ Чек-лист: всё ли идёт хорошо?

- [ ] Процесс жив (PID 11791)
- [ ] Loss падает (проверить последние 10 значений)
- [ ] Лог растёт (`ls -lh results/bert_training_log.txt`)
- [ ] Нет ошибок в логе
- [ ] RAM/диск в норме
- [ ] GPU/CPU работает

Если всё галочками → **отдыхайте, модель учится!** 🎉

---

**Документ создан**: 1 февраля 2026
**Последнее обновление**: в процессе обучения
**Статус**: ✅ ACTIVE (PID 11791)

---

## 📚 Полезные ссылки

- [HuggingFace BERT documentation](https://huggingface.co/docs/transformers/model_doc/bert)
- [PyTorch MPS backend](https://pytorch.org/docs/stable/mps.html)
- [Transformers training](https://huggingface.co/docs/transformers/training)

---

**Команда для быстрой проверки всего сразу**:

```bash
echo "=== Статус процесса ===" && \
ps aux | grep run_bert_training | grep -v grep && \
echo "" && \
echo "=== Последние 5 строк лога ===" && \
tail -5 results/bert_training_log.txt && \
echo "" && \
echo "=== Размер лога ===" && \
ls -lh results/bert_training_log.txt && \
echo "" && \
echo "=== Модель создана? ===" && \
ls -la models/bert_category_classifier_cleaned/ 2>/dev/null || echo "Ещё не создана"
```

Скопируйте и вставьте в терминал — увидите всю картину! 🚀